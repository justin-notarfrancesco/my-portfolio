# Umami Cloud website ID — lives in the Vercel project's env settings (Production), not locally.
# The tracker only renders in production builds, so leave this unset in local dev.
PUBLIC_UMAMI_WEBSITE_ID=
